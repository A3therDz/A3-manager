# 安全说明

## 这个程序会碰什么

- **只读**你的图库:扫描图片、读取 PNG 元数据、生成缩略图缓存到图库内的 `.comfy-thumbs/`。
- 索引库与设置写在 `%APPDATA%\A3 manager`,不写进程序目录。
- 删除图片是"移入系统回收站",不做不可恢复的删除。
- 唯一的网络行为:点击设置里的作者/仓库链接时,用系统浏览器打开。

## 报告安全问题

如果你发现会导致数据丢失、越权读写文件、或意外联网的问题,请**不要**开公开 Issue,
改发邮件或在仓库里开一个 Draft Security Advisory(GitHub 的 Security → Advisories)。
